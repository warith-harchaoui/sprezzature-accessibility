name: ci

# Gentle, deterministic gate: ruff lint + pytest on the supported Python
# versions. No model calls, no network. Intentionally minimal.

"on":
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          # The package editable, with the surfaces it advertises: without
          # the extras every test of the HTTP and MCP surfaces skips, and CI
          # was green on code nobody ran.
          pip install -e ".[lang,api,mcp]" -r requirements-dev.txt
      - name: Ruff
        run: ruff check .
      - name: Pytest
        run: pytest -q
